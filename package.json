{
  "name": "snake",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.ts",
  "scripts": {
    "clean": "rm -rf ./build/*",
    "compile": "tsc --project tsconfig.json",
    "dev": "npm run compile && parcel ./src/index.html",
    "build": "npm run compile && parcel build ./src/index.html"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "typescript": "^3.7.5"
  }
}
